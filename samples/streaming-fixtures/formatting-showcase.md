# Formatting showcase

Inline sampler: **bold**, _italic_, ~~strikethrough~~, `inline code`, [named link](https://example.com/formatting), <https://example.com/autolink>, citation[^formatting-sample], and math $f(x) = x + 1$.

## Inline combinations

Streaming text may include ~~deleted wording~~ next to **strong wording**, _emphasized wording_, and `inline identifiers` in the same paragraph.

## Ordered start

4. Continue from a previous answer.
5. Preserve the explicit starting index.
6. Keep later items aligned while chunks arrive.

## Divider

The content above this paragraph should remain separated from the follow-up section.

---

The content below the divider should render as a new section, not as part of the previous paragraph.

## References

This final paragraph keeps the stream moving after the formatting-heavy section and includes a source marker [^formatting-sample].

[^formatting-sample]: Formatting showcase source
