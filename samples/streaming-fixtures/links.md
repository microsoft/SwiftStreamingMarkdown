# Links

Inline sampler: **bold link text**, _italic link text_, `href`, [link sample](https://example.com/link), citation[^link-sample], and math $u = v$.

## Complete link

A complete link should use the host link callback: [Open documentation](https://example.com/docs).

## Split link

A split link like [partially streamed link](https://example.com/streaming) should become interactive only after both label and URL are complete.

## Longer paragraph

Longer link-heavy answers often include explanatory text before and after the link. This extra paragraph lets the demo show plain text first, then the link label, then the URL, and finally the action callback once parsing succeeds.

## Final link

One more public link to [CommonMark](https://commonmark.org) appears near the end so users can test the callback after the stream has been running for a while.
